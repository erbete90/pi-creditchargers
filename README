Compile Using
$ gcc app.c -o app.o -lwiringPi -lwiringPiDev `mysql_config --cflags --libs`

Make sure  MySQL server already installed on raspberry pi.
Create database "app", import app.sql by using this command:
$ mysql -u root -p app < app.sql
